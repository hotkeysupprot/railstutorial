<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) # 自分が管理者であり、自分自信を消すので無い限り削除リンクを表示する %>
      | <%= link_to "delete", user, method: :delete, # ブラウザはネイティブではDELETEリクエストを送信できないため、RailsではJavaScriptを使用してこれを偽造します
                    data: { confirm: "You sure?" } %>
  <% end %>
</li>